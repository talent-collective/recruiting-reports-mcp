services:
  - type: web
    name: recruiting-reports-mcp
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py streamable-http $PORT
    envVars:
      - key: PORT
        value: 8000
